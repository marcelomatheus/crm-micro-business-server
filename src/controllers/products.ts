// import express from "express";
// const productsRouter = express.Router();

// productsRouter.get("/", service);

// productsRouter.get("/", service);

// productsRouter.get("/:id", service);

// productsRouter.post("/", service);
// productsRouter.put("/", service);
// productsRouter.delete("/", service);

// export default productsRouter;
