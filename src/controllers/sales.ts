// import express from "express";
// const salesRouter = express.Router();

// salesRouter.get("/", service);

// salesRouter.get("/", service);

// salesRouter.get("/:id", service);

// salesRouter.post("/", service);
// salesRouter.put("/", service);
// salesRouter.delete("/", service);

// export default salesRouter;
